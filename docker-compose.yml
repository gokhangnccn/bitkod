services:
  code-runner:
    build:
      context: ./docker/code-runner
    container_name: code-runner
    stdin_open: true
    tty: true
    volumes:
      - ./runner:/app
  redis:
    image: redis:latest
    container_name: redis
    ports:
      - "6379:6379"
    restart: always

